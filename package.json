{
  "name": "loginregistration-root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "npm --prefix ./client run dev",
    "client:dev": "npm --prefix ./client run dev",
    "client:build": "npm --prefix ./client run build"
  }
}
